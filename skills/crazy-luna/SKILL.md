---
name: crazy-luna
description: 为开发任务生成有事实依据的修改方案、子 Agent 分工、验收标准和启动 Prompt，并按当前宿主能力选择配置中的模型。
disable-model-invocation: true
---

# CrazyLuna

将开发任务整理为可独立执行和验收的交接方案。采用用户的语言，默认交付方案文档和聊天中的启动 Prompt；生成 Prompt 不启动业务执行或 Goal。用户明确要求继续执行时，按已有授权推进。

执行者依据交接方案工作，无需重新调用本 Skill。

## 1. 查证需求与范围

先确认具体任务和目标项目，读取适用的项目指令，查明写入范围；任务或项目不明确时先询问，不创建文档或状态目录。读取权威需求、相关入口、代码、测试和配置，确认当前行为、预期结果、实现缺口、非目标及必须保留的契约。记录源码基线和已有改动；没有 Git 时使用实际文件信息。

区分事实、已确认决策、假设和待决策项。原始需求及已确认决策完整记录或提供可访问引用，为设计结论标明依据，使执行与验收不依赖规划对话。用户纠错时删除错误前提，维护当前有效方案。

常规实现选择由规划者根据证据解决；影响产品语义、范围或授权的缺失信息才询问用户。无法访问必要资料时说明证据缺口，按影响范围处理：

- **局部阻塞**：标记受影响任务、后续依赖及解锁条件，只为不受影响的任务生成可执行方案和启动 Prompt。
- **整体阻塞**：缺口影响整体目标或没有独立可执行部分时，只交付事实基线、待决策项及影响，不生成占位任务卡或开工 Prompt。

## 2. 设计与任务拆分

按任务的依赖、风险和不确定性调整方案深度，给出一条有依据的实施路径，写清修改位置、调用或数据流变化、具体规则、边界行为和复用点。关键架构与语义判断在派发前完成；执行 Agent 可处理不改变既定设计和契约的实现细节。

- **小范围、逻辑明确**：一个实现与自测任务，另做独立验收，用简短方案说明改动和验证。
- **多个独立交付物**：分别拆任务，明确依赖和共享修改点。
- **大规模重复工作**：共用处理规则与验收要求，按目录、模块或数据批次组织，不逐文件复制任务卡。
- **跨阶段或存在未知因素**：先确定整体目标、需求覆盖、关键契约、阶段依赖和完成条件，再细化就绪阶段；后续阶段记录待解决问题、解锁条件及补齐设计的责任人。

仅在能减少上下文负担、明确写入责任或形成独立可验证产物时拆分。使用稳定任务 ID（如 `T01`），每项写清以下信息；小任务可用短段落，共同信息引用一次即可：

| 字段 | 内容 |
| --- | --- |
| 目标与交付物 | 对应需求和验收 ID、可核对的结果 |
| 输入与依赖 | 文件/符号、资料、前置产物、解锁条件及阻塞项 |
| 修改范围与步骤 | 允许修改或新增的位置、唯一写入者、已确定的实施规则 |
| 验证 | 工作目录、环境前提、命令或操作、预期结果 |
| 执行约定 | 角色、模型及实际推理参数、必要工具、停止条件 |

无冲突的就绪任务可并行。重叠写入、未稳定契约、互斥运行资源和真实前置依赖需要串行；共同读取或使用稳定契约不构成串行理由。共享修改点指定唯一写入者，必要时安排集成任务。

相关测试随实现安排，优先复用或扩展现有测试；新增测试对应未覆盖行为或明确回归风险。只有独立工作或验证价值才另拆测试任务。

## 3. 模型与宿主能力

读取 [模型清单与宿主规则](references/models.md)，以其作为候选配置、选择优先级、参数解析和本地记录格式的唯一来源。宿主可提前只读探测；在任务的工具、输入类型和上下文需求明确后，解析各角色的精确模型 ID、实际推理参数、选择依据、限制及已授权的失败后候选选择规则，写入方案。

按上述 reference 的复核与持久化规则更新 `.crazy-luna/models.local.json`，在方案中说明未解析能力或未保存的结果。

宿主没有子 Agent 派发能力时，改为分别供实施与独立验收使用的手动会话 Prompt，写清顺序和用户推进步骤。模型或参数尚未确认时列出开工前置条件，不把交接资料标为可自动执行。

## 4. 执行与验收协议

将本节适用规则写入交接方案。

### 分工与派发

- **协调者**：统一派发、集成、维护执行记录并处理升级。
- **任务角色**：按任务能力、输入、产物及读写范围设置；职责相近且无分离需要时合并，角色数量与 Agent 实例数量分别按职责和就绪工作量确定。
- **独立验收者**：由具备相应能力、未参与被验收产物制作的 Agent 担任，在独立上下文核对权威资料及实际产物，不只采信制作方摘要，不修改验收对象。可合并验收同批任务。

子 Agent 向协调者回报，默认由协调者直接派发，不增加嵌套层级。

启动前复核方案中的派发接口、模型、推理参数、上下文隔离和并发能力，传入任务卡、权威资料及必要上下文。记录请求模型与宿主实际回报，未回报则标明未知。能力变化或型号不符时报告差异，按已授权的候选规则处理；单一指定模型不可静默替换。

并发受独立就绪任务数、用户指定的上限和宿主剩余名额共同约束，按宿主口径计算占用；上限未知时先串行。

### 验收标准与证据

为验收标准设置稳定 ID（如 `AC01`），明确前置条件、输入/操作、预期结果、验证方式及证据，维护 `需求 → Txx → ACxx → 证据` 的对应关系，保留独立需求及层级。

1. 核对方案与标准是否覆盖原始需求及已确认决策。遗漏、矛盾或错误交由协调者按升级规则修正，不能以符合错误方案判定通过。
2. 停止对待验收产物的写入，用提交、文件摘要或快照标识实际内容；有未提交修改时不能仅用 HEAD。其他不冲突任务可继续。
3. 独立检查实际差异并运行必要验证，记录产物版本、逐项 AC 结果、命令/操作及证据。区分静态检查、测试和实际运行，未执行项标为未验证。
4. 交付前核对产物与验收版本一致。产物或前置依赖变化时，协调者确定受影响的任务和 AC，更新其状态并安排必要的重新验收；影响尚未查明时，暂停使用已变化输入的任务及其完成判定。

只有范围内任务经独立验收、最终集成状态满足全部必需 AC、本轮引入的差异均属于授权范围，且基线已有的无关改动保持原样、证据完整，才能宣布目标完成。必需验证缺失或只完成部分就绪任务时，报告已完成部分与剩余阻塞；范围调整须有用户明确授权。

### 进度、恢复与升级

协调者维护唯一执行记录，包含 `任务 ID | 负责人/模型 | 状态 | 产物版本/证据 | 阻塞原因/下一步`。状态为 `TODO → RUNNING → VERIFYING → PASS`；失败进入 `REWORK`，缺条件进入 `BLOCKED`。`PASS` 必须有独立验收证据，返工或解锁后按流程恢复。

子 Agent 回报任务 ID、产物、验证结果、证据及阻塞。协调者在派发、验收、返工、阻塞和完成时更新记录并简短汇报。续接先核对方案、记录、实际工作区和在途 Agent，避免重复派发；规划阶段仅指定记录位置，不创建空记录。

有新证据且修复持续收敛时，继续处理范围内缺陷；需要预算控制时按任务设置重试上限。重复失败无新证据、修复不再收敛、设计前提失效或需要扩大范围时，暂停受影响任务，回报期望与实际、复现证据、已尝试内容及待解决问题，其他任务继续。

有相应能力与授权的协调者可在既定范围和产品契约内修订方案、补齐已解锁阶段；否则交回规划者或用户，相关任务保持阻塞。修订需更新方案版本、受影响任务及验收状态，不擅自扩大模型候选范围或放宽验收要求。

## 5. 交付方案与启动 Prompt

按项目文件规则保存方案。小任务用简短文档，多任务附任务信息；跨阶段按需拆分总方案与阶段方案，明确入口和阅读顺序，在总方案统一追踪需求。已有资料引用复用，不填充空章节。

交付前确认：执行者无需规划对话即可找到目标与非目标、权威需求、事实基线、设计、任务依赖、角色模型、验收条件、执行协议及首批任务；每条需求有任务和验收对应，阻塞项有影响范围及解锁条件。

聊天中给出方案链接、可执行范围、阻塞项及环境待查项。有就绪任务时提供填好真实信息的启动 Prompt；以下为自动派发场景的骨架，按当前能力调整，交付时不得保留占位字段：

```text
请依据以下交接方案完成本轮范围内的全部任务，按依赖逐批推进。
目标：<原始目标>
项目：<实际路径>
权威方案：<可访问的位置及版本>
执行记录：<按项目规则确定的位置，执行开始时创建或续用>
本轮范围与首批任务：<执行范围及首批 Txx>
剩余阻塞：<受影响任务及解锁条件，无则写无>

你担任协调者。先读取项目指令、方案及已有记录，复核工作区基线、在途任务和宿主能力。
使用经复核的宿主接口，按方案的角色、模型及实际推理参数派发就绪任务，遵守依赖、写入归属与并发限制。
将对应任务卡、原始需求及已确认决策的来源、前置产物和必要上下文传给各 Agent。
验收前停止对待验收产物的写入并标识实际版本，安排未参与该产物制作的 Agent 独立验收。
验收者核对原始需求与已确认决策的覆盖，检查实际产物并运行必要验证，逐项记录 AC 结果，将证据绑定验收版本；未执行的验证如实标明。
维护唯一执行记录，在派发、验收、返工、阻塞和完成时更新状态并简短汇报；前置条件满足后继续派发后续任务。
按方案处理返工、升级和依赖变化，更新受影响任务及验收状态；续接时核对记录、实际工作区和在途 Agent，避免重复派发。阻塞时说明影响及解锁条件，继续不受影响的任务。
报告实际交付、逐项验收结果、证据及剩余阻塞；交付前核对产物与验收版本一致，最终集成状态满足全部必需验收后才能宣布目标完成。
```

用户明确要求使用 Goal 且宿主支持时，才补充经核实的 Goal 启动方式，以最终业务结果和必需验收为完成条件；未指定 token 预算则不设置。方案就绪、运行能力确认与业务验收通过分别报告。
