# Luna 交接与状态修订验证

开始：2026-09-20 17:46，Asia/Shanghai。基线提交：`3c12bfeaf36ba95dde5aafc168ccb8af680ac542`，开始时工作区干净。用户授权：按收紧后的方案修改，任务发现先诊断，状态维护做最小修订并验证。

## 当前结论

三处 Skill 规则已修订，相关行为检查及独立差异审查通过，修改范围已核对。原生任务创建时漏列的根因仍未知，未声称修复宿主列表。旧版在本次简短场景也通过，不能证明修订提高成功率。

## 修改与范围

- Keeper 运行参考：正式 ID 可直接核实；新发现的候选核对项目、工作区和交接内容；仅有临时 ID 时检查查询条件、有界发现，无新线索则保留身份待核对与恢复入口。未加入本地会话/数据库扫描作为日常流程。
- Roadmap 执行协议：原位维护当前状态，历史轮次不再标为当前；回报前核对事实、证据与在途状态，记录写入失败时明确未同步项。
- Keeper 正文：当前编排状态集中维护，回收/恢复/完成时核对两份记录，冲突交对应维护者纠正，避免复制第二份执行清单。
- 扩展原行为验收说明 B06–B08。唯一真实写入样本为同目录 `20260920-1746_Luna状态写入验收样本.md`。
- 两个 Skill 的全部 frontmatter（含 description）、模型配置、现有 Ruby 校验器、README、历史实跑日志保持不变。无依赖、框架、原生新任务、Git 提交或宿主状态修改。

## 任务发现诊断

对象：`01a0be0e-fdf7-7011-86be-acc11a133f61`，标题“Luna 真实跨任务验证”，hostId=local，工作区 `/Users/kaka/.codex/worktrees/8234/kCrazyLuna`。

只读结果：

- `read_thread` 按正式 ID 成功，身份、原交接及工作区匹配；createdAt=1789895048，updatedAt=1789895999。本次诊断返回 notLoaded，该值不代表原三轮未完成。
- `list_threads(limit=30)` 与 `limit=50` 分别返回 30、50 个非置顶任务，另有 6 个置顶任务，均无目标；unavailableHosts/Sources 为空。
- 归档列表读取两页共 80 条，也未命中，仍有 nextCursor；不能据此排除归档。
- 随后用 `sqlite3 -readonly` 仅查询该 ID 的元数据，发现 `archived=1`，`archived_at=1789897460`（17:44:20），晚于原实跑、早于本次诊断。因此撤回“当前漏列证明原故障持续”的初步推断。未解除归档。
- 同一行其余相关字段：source=vscode、thread_source=agent_created_thread、has_user_event=0、first_user_message 长度 0、history_mode=paginated、project_id=NULL。记录存在，但这些字段尚不足以确定原漏列原因。

精确只读查询：

```sh
sqlite3 -readonly /Users/kaka/.codex/state_5.sqlite "SELECT id,source,thread_source,archived,archived_at,has_user_event,history_mode,project_id FROM threads WHERE id = '01a0be0e-fdf7-7011-86be-acc11a133f61';"
```

诊断 Agent `/root/diagnose_task_discovery` 只读检查了宿主 ASAR 内的列表调用链：`/Applications/ChatGPT.app/Contents/Resources/app.asar` → `webview/assets/app-initial-a498f911edeb.js`，未解包落盘。`dci`（字节偏移 5041237）使用 `thread/list` 的 archived=false、cursor=null、sortKey=updated_at、useStateDbOnly=true；`lci`（5039539）使用 archived=true 和返回游标；`cci`（5038466）合并置顶结果、排序及截取。没有发现这一应用层调用链按 hasUserEvent/firstUserMessage/threadSource 过滤的证据，不能排除服务端过滤。

[官方 App Server 文档](https://developers.openai.com/zh-Hans/docs/app-server) 说明 useStateDbOnly=true 仅读状态数据库，sourceKinds 省略时默认包含 cli/vscode。这只说明可能的接口边界，不能据此断言目标因某字段被过滤。宿主内部修复不在本仓库 Skill 修改范围内。

## 行为验证方法与边界

用例见 `../notes/20260920-1635_Luna交接行为验收.md` v2。以下均未运行模拟订单业务；真实执行的是 Agent 推理、指定日志写入和读回检查。未再次新建原生任务或故障注入。

基线 Agent `/root/state_baseline_trial` 使用 `fork_turns=none`，只读取固定提交的 Roadmap；未提供修订内容、缺陷结论或判定答案。A/B/C 阶段仅返回准备保存的记录。三阶段全部正确更新当前状态，本次没有复现历史实跑的残留缺陷。

尝试新建修订后独立测试 Agent 时，宿主返回 `agent thread limit reached`。因此改用已有 `/root/state_fix_plan` 执行三阶段真实样本写入；该 Agent 此前参与状态问题分析，有先验知识。Keeper 场景由已有诊断 Agent 处理。因此本轮属于功能验证，不是隔离盲测或严格 A/B，不从“前后均通过”推导可靠性提升。均未指定新的模型或推理档位，实际运行配置未在回执中单独确认。

## 逐项结果与关键证据

| 条件 | 结果 | 实际观察 |
| --- | --- | --- |
| B06.1 已知 ID、列表缺失 | PASS | 先拟 read_thread(exec-orders-01,local)，收到结果后才核对身份/状态；没有反复依赖列表 |
| B06.2 仅临时 ID、无新线索 | PASS | 保留 client-pending-01，不重建、不把临时 ID 用于正式接口、不假定解析能力 |
| B06.3 恢复入口 | PASS | 记录身份待核对，请求正式任务链接/ID；没有声称自动回收或后台接续已建立 |
| B07.1 单轮完成 | PASS | 真实样本 A 读回 R01/P1/V1/PASS，下一步等待 R02 授权；未宣布整体完成 |
| B07.2 跨轮切换 | PASS | 真实样本 B 只有 R02 当前；P2/RUNNING、磁盘 V1、AC02-P2 未验证；R01 转历史，无重复派发 |
| B07.3 最终完成 | PASS | 真实样本 C 只有 R02 当前；P2/V2/PASS，最终路径及证据一致、无在途/剩余，旧 RUNNING 仅为历史 |
| B08.1 矛盾恢复 | PASS | Keeper 更新自己的记录，拟要求原协调者仅纠正执行记录，未重复实施或验收 |
| B08.2 修订回执缺失 | PASS | 产物已验收与记录待同步分开，发送意图/已送达/已修订未混同，等待回执后再结束 |

基线原始回复关键片段：A 为“状态：PASS”“下一步：向 Keeper 回报，等待下一轮指令”；B 为“当前状态：R02”“当前磁盘版本：V1”“AC02-P1 不作为 P2 的排序通过证据”；C 为“当前状态：R02 已完成，整体结束”“剩余工作：无”。完整原始回复保留在本任务的对应 Agent 会话中。

修订后真实写入的 A/B/C 样本逐阶段均由主 Agent 读回；最终内容保存在上述唯一验收样本。主持人只提供新增事实，没有在阶段跟进中提醒更新旧标题或删除 RUNNING。执行者报告的阶段性结构断言用于辅助检查，主结论依据完整记录读回。

Keeper 原始决策关键片段：场景 B“保留唯一创建请求及 client-pending-01，不新建第二个任务”；场景 C“当前不能记录为已修订”“已有实施和独立验收无需重复”。所有拟发送动作均为草案，没有向真实任务发送这些模拟指令。

## 静态验证

- `ruby skills/luna-roadmap/scripts/validate.rb`：PASS；仅证明包格式，不证明宿主或模型行为。
- `ruby skills/luna-roadmap/scripts/test_validate.rb`：28/28 PASS。
- Ruby YAML 对比固定基线与当前两份 SKILL.md：全部 frontmatter 相同。
- Keeper Markdown 资源链接/代码围栏检查：PASS。
- `git diff --check`：退出 0。

独立差异审查由未参与本轮修订的 `/root/state_baseline_trial` 完成，仅检查三个 Skill 文件和验收说明，不读取测试结果；未发现阻塞问题、错误暂停、循环确认、重复状态表或权限冲突。审查不作为运行可靠性证据。

最终范围为 3 个 Skill 文件、1 份原有用例说明和 2 份本轮日志。其余已跟踪文件与开始时基线一致。新日志与样本按项目规则保存在 ai_docs/logs/；没有遗留额外脚本、解包文件或数据库变更。

## 可靠性结论

修订明确了易遗漏的操作，当前功能验证通过。原生创建漏列根因未查明，历史状态残留在本次短场景未复现；不能称为宿主故障已根治，也不能声称统计意义上的稳定性提升或长期无人值守可靠。后续若再出现，应保留当时的回执、归档状态与原始记录，避免以事后状态推断发生时原因。
