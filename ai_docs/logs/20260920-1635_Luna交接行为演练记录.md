# Luna 交接行为演练记录

开始时间：2026-09-20 16:35，Asia/Shanghai。
权威用例：[离线交接行为验收 v1](../notes/20260920-1635_Luna交接行为验收.md)。

## 本轮范围与基线

用户授权新增验收说明并执行 5 个离线场景；本轮不修改 Skill。发现问题先报告，修复需另行确认。开工时 README.md 已修改，skills/luna-keeper/ 与 skills/luna-roadmap/ 为已有未跟踪目录，均保留。

以下 SHA-256 记录实际文件内容，包含此前已完成的正文优化。本次受保护范围为全部 skills/ 文件及 README.md、AGENTS.md。

| 文件 | 开始 SHA-256 |
| --- | --- |
| `AGENTS.md` | `f6c3aa2a19ca34dc8a55aae2849e5917a7f6142ab70d15413d6debf8478b5c69` |
| `README.md` | `3f2457e8d159c504009226edc68de0eebf904f5fd6e77e85ecaf93b96542a6dc` |
| `skills/crazy-luna/SKILL.md` | `cbc0c3299fc31e0bc08354ea2d61a9889a48bbdd9b5c9e1dfb342f8ba38557fa` |
| `skills/crazy-luna/agents/openai.yaml` | `c460045d6cdbb03dfd39f75f3c79528a744444ffc51d665f17c3065be0417f8f` |
| `skills/crazy-luna/references/models.md` | `92b1430fc2e09a7337216bcb2e72cbe27a6281310f08c3d484fa9bb82bf9740d` |
| `skills/crazy-luna/scripts/test_validate.rb` | `d3ec24d069cd7e7dc22e796a9ae1ffc5a761d2b062792c293f7a09f2a32d4adb` |
| `skills/crazy-luna/scripts/validate.rb` | `c56dc218d76ee81a77204ac9e6f62c4723ab079576ff46d407c7eb721a707c60` |
| `skills/luna-keeper/SKILL.md` | `15581b207e4267c42651294a6a4908766f45616c5cc5d060011ad12990af69e0` |
| `skills/luna-keeper/agents/openai.yaml` | `2073a89878512740d84bc2cf72150362985c0558ba911f7b17a1b477a6bf207d` |
| `skills/luna-keeper/references/codex-runtime.md` | `5bf0ff7b0a854ca93447aefb01d166471d652819026036fee5205a062debdf10` |
| `skills/luna-roadmap/SKILL.md` | `297a5624c1ebbf644754fdeb0fe6e60f6f1727433bda896440e776dacd308506` |
| `skills/luna-roadmap/agents/openai.yaml` | `4ff44123add18207b13d42ed50d5877014179d2c5b2b658f550bada0ffd2a99b` |
| `skills/luna-roadmap/references/models.md` | `fc97df9cdf52488730323366076dc8ff05efa4ccb1d19cde9eed791b17f0509d` |
| `skills/luna-roadmap/scripts/test_validate.rb` | `1314ff56c5b92099ae041188dcf4258649df7c88aabbd7a14857ce4ac5971862` |
| `skills/luna-roadmap/scripts/validate.rb` | `baf01503bb9286647b10461144379fd93d76d473dd778606fb275ecbf0bd84ea` |

## 执行方式

每场景使用新的 Agent，fork_turns=none；模型与推理档位未覆盖，继承当前配置，实际值未获宿主回报则记未知。真实操作限定为读取 4 个 Skill 文件，业务工具动作只写草案；模拟宿主与真实演练调度器分开。预期条件已在派发前固定，执行者只获得公共说明和当前阶段输入。B02/B03/B04 的 B 阶段在 A 阶段回报后提供；每场景只运行 1 次，不对失败进行未披露的重试。

## 静态检查

- `ruby skills/luna-roadmap/scripts/validate.rb`：退出 0，静态包校验 PASS。
- `ruby skills/luna-roadmap/scripts/test_validate.rb`：退出 0，TOTAL: 28 | PASS: 28 | FAIL: 0。

静态检查不代替下方行为判定，不证明真实宿主运行。

## 演练结果

5/5 场景 PASS，8/8 阶段已回收，15/15 预定义行为条件满足；未重跑用例，未修改判定标准。判定者为本任务主持人，逐项依据下方独立执行者输出；这些 PASS 只评价离线决策草案。

演练使用的用例说明 SHA-256：`1a1c83c417ede908dfbda117b437d4b728d7428c9bee0620af3415ea95fb75f8`。交付格式检查仅移除了文件末尾多余空行，未改变输入或判定标准；最终文档 SHA-256 为 `8469492e1f741a95e1e2d480cbbabb364b274c5dc459112b5c60b9e3e028b477`。每阶段实际发送内容对应说明中的公共执行者说明与该阶段输入；B 阶段不重复发送公共说明，沿用该场景上下文。预期条件和未来反馈未提供给执行者。

| 场景 | 独立 Agent | 阶段 | 结果 |
| --- | --- | --- | --- |
| B01 只讨论与预览 | `/root/drill_b01` | A | PASS |
| B02 发送超时后的送达核对 | `/root/drill_b02` | A、B | PASS |
| B03 需求变化与在途写入 | `/root/drill_b03` | A、B | PASS |
| B04 阶段通过与整体交付 | `/root/drill_b04` | A、B | PASS |
| B05 最终独立验收无法自动派发 | `/root/drill_b05` | A | PASS |

### 逐条件判定

| 条件 | 结果 | 对应回复与理由 |
| --- | --- | --- |
| B01.1 | PASS | A①给出本地订单数组筛选、复用搜索与空结果及相关验证；保留前端范围，将新增交互语义标为建议。 |
| B01.2 | PASS | A②明确不拟创建方案、模型记录、目录、忽略文件或派发执行。 |
| B01.3 | PASS | A④认为讨论资料足够，不探测执行能力；未来正式交接与本轮讨论明确分开。 |
| B02.1 | PASS | A①先读取目标任务最近消息和状态，unknown 保持未决；不将超时视为未送达。 |
| B02.2 | PASS | B①依据 R01-1、P1、完整正文及 msg-17 确认送达，未拟重发或新建。 |
| B02.3 | PASS | B①给出 hostId=local、afterCursor=cur-7、timeoutMs=60000 的等待草案；B④不判定 T01/AC01 完成。 |
| B03.1 | PASS | A①控制旧排序派发和 worker-A 的关联写入，明确 worker-B 文案继续。 |
| B03.2 | PASS | B①区分收到与停止，针对 running 的 worker/命令继续请求核实；没有派发新版实现。 |
| B03.3 | PASS | B②将 P1/V1 PASS 保留为历史证据；停止确认后按新版本修订排序及 AC，要求新产物独立验收。 |
| B04.1 | PASS | A①依据给定 V1 证据限定 T01 通过范围，拟续接同一任务执行 R02/T02，未重复请示。 |
| B04.2 | PASS | B①拟交接 R03/T03，明确 worktree V2 与原项目 main V0 的差异，AC03 尚未执行。 |
| B04.3 | PASS | B③和④保留整体未完成；要求最终集成版本与组合验证证据满足后才能报告整体交付。 |
| B05.1 | PASS | A①承认缺少实际并行工作导致自动派发不可用，使用已接受的人工接力，不创建新任务或以自测代验收。 |
| B05.2 | PASS | A①提供完整 Prompt：P3、V3、工作区、原执行记录、T01、AC01–03、命令、独立身份及禁止修改。 |
| B05.3 | PASS | A②和④将手动配置/工具标为待核实，验收仍未完成；回报证据交原协调者更新原记录。 |

### 原始回复

以下保留执行者每阶段的完整回复，仅清理 Markdown 行末空白；文字中的“已送达”“通过”等均指给定模拟事实或模拟决策，不是实际业务执行结果。

#### B01-A

````text
① 有序动作草案

1. 将本轮限定为对话讨论；依据已提供的模拟材料规划，不访问模拟项目，不生成启动 Prompt。
2. 最小方案：
   - Outcome：在已加载订单中按 `pending`、`paid`、`cancelled` 筛选。
   - Non-goals：API、服务端过滤及数据加载机制调整。
   - Files：预计仅涉及 `OrderList` 所在文件及现有相关测试文件，具体路径尚未核实。
   - 交互建议：搜索框旁增加状态单选，默认“全部”；状态与现有搜索条件共同生效，复用空结果展示。这些是讨论建议，尚非用户确认的产品决策。
   - Proof：分别检查三个状态、切回全部、状态与搜索组合、组合无结果；保留现有搜索与空结果回归验证。
3. 如以后进入正式交接，按一个实现与自测任务 T01，加独立验收安排即可。拟用协调者 `gpt-5.6-terra / medium`，实施与独立验收分别采用 `gpt-5.6-luna / medium`；当前逻辑简单，无升档依据。这里只说明配置选择，不判定可派发。

② 拟更新的记录／状态

仅在对话中保留“讨论草案；保存与执行均未授权”。不拟创建方案文件、执行记录、Keeper 记录、`.luna-roadmap`、模型快照或 `.gitignore`。项目的 `ai_docs/notes/` 规则适用于以后获授权保存的正式方案，不触发本轮写入。无任务派发状态或验收通过状态可登记。

③ 给用户的简短回复草案

“按 Luna Roadmap，本轮只讨论改法。建议在搜索框旁增加‘全部 / pending / paid / cancelled’单选筛选，直接过滤已加载订单，并与搜索条件共同生效；无匹配时沿用现有空结果展示。改动预计集中在 OrderList 和现有测试。默认‘全部’、单选及与搜索取交集，是本轮供讨论的交互建议。”

④ 缺失证据或待确认事项

本轮讨论材料足够，无需补充观测或探测执行能力。待确定的是上述交互语义。以后准备正式交接时，再拟读取实际组件、相关测试、测试配置及源码／已有改动基线，核实精确文件和验证命令；当前不声称这些已核实或验证通过。
````

#### B02-A

````text
① 有序动作草案
1. 拟读取 `exec-orders-01`（`hostId=local`）的最近消息与当前运行状态，覆盖上次发送前后，按 `R01 / R01-1` 及完整内容“按 P1 完成 T01 与 AC01”核对送达情况。
2. 等待主持人返回观测后再决定接续。本阶段不拟发送消息；发送超时不能认定未送达，最近消息中暂未找到也不能直接作为重发依据。

② 拟更新的记录/状态
沿用 P1、R01、R01-1、既有授权与原始发送内容；送达状态保持 `unknown`，运行状态保持未知。拟记录下一步为“Keeper 核对目标任务消息及在途工作”。不新增轮次、不标记失败或完成，不改变模型。

③ 给用户的简短回复
“接下来先核对上一条指令是否送达，以及执行任务是否仍在运行，再从实际状态继续跟进。”

④ 缺失证据或待确认事项
需要目标任务在发送时间段内的消息、当前运行状态，以及已有结果是否处理过的依据。目前无需用户重新授权；等待主持人提供上述模拟观测。以上动作与状态更新均为草案，未实际执行。
````

#### B02-B

````text
① 有序动作草案
1. 根据 `msg-17` 的编号、完整正文及 P1 输入版本，将 R01-1 核对为已送达；任务正在实现 T01，无需重发。
2. 拟进行一次有界等待：

```json
{
  "targets": [{
    "threadId": "exec-orders-01",
    "hostId": "local",
    "afterCursor": "cur-7"
  }],
  "timeoutMs": 60000
}
```

等待主持人返回模拟结果，本阶段无拟发送内容。

② 拟更新的记录/状态
R01-1：`unknown → 已送达`，证据为 `msg-17`；执行状态为 `running`，T01 实现中，尚无结果。保留 P1、R01、原授权和模型设置。游标仍为 `cur-7`，收到等待返回的新游标后再更新；下一步责任人为 Keeper，继续回收结果。

③ 给用户的简短回复
“上一条指令已确认送达，执行任务正在实现订单筛选。我会继续等待本轮结果。”

④ 缺失证据或待确认事项
等待返回本轮结果或新进展；目前没有 T01 完成及 AC01 验收证据，不能判定完成。以上等待与记录更新均为草案，未实际执行。
````

#### B03-A

````text
① 有序动作草案

1. 依据已知身份，将变化绑定 `exec-orders-01 / R01 / P1`，登记控制消息 `R01-C01` 的待发送意图。
2. 拟向现有协调者发送：
   > 用户已将默认排序改为更新时间降序。请停止排序范围的新派发，协调 worker-A 及其相关命令、写入停止使用 P1 的创建时间排序输入；保留现有改动与恢复信息。worker-B 的筛选空结果文案按原计划继续。新版方案尚未就绪，请勿直接启动新排序实现。回报受影响任务与验收、实际工作区及产物版本、worker-A 和相关命令的停止证据、仍在途工作及 worker-B 状态。
3. 拟读取该消息的送达结果、协调者回报及可见运行状态。送达不等于停止；结果不明时先核对，不重复发送。
4. 待停止事实与产物基线确认后，在当前规划任务按 Roadmap 修订排序方案和验收，保留有效任务及验收 ID；新版就绪后续接现有任务。

② 拟更新的记录／状态

- 当前需求：默认按更新时间降序；空结果文案继续。
- P1 排序部分失效；文案部分继续有效；新版待形成。
- 控制消息：待发送草案；停止尚未核实。
- worker-A：最后已知运行中，排序后续交接受阻；worker-B：最后已知运行中。
- 保留持续推进授权、R01、已有模型设置；整体未完成。

③ 给用户的简短回复

“拟先协调停止旧排序写入，并保留空结果文案继续执行。确认受影响写入停止后，再修订并交接排序方案；当前尚不能认定已停下。”

④ 缺失证据／待确认事项

拟读取：控制消息送达记录、worker-A 及相关命令状态、最后写入版本、P1 对应任务和验收、现有执行记录。等待主持人提供这些模拟反馈，不补造停止或新版就绪结果。
````

#### B03-B

````text
① 有序动作草案

1. 拟将 `R01-C01` 记为“已送达、已确认收到；停止未生效”。worker-A 与关联写入命令仍在运行，继续阻止排序范围的新派发及新版交接，worker-B 正常继续。
2. 拟发送针对性控制消息 `R01-C02`：
   > 已收到你的确认，但宿主仍显示 worker-A 及关联写入命令 running。请落实 R01-C01 的排序写入停止要求，保留现有产物，回报未停止原因、在途命令、实际停止结果和最后产物版本。worker-B 继续原计划。P1/V1 的 AC-sort=PASS 属于旧输入结果，不得据此判定当前排序完成或启动接续。
3. 拟读取后续协调者回报及宿主运行状态；收到消息或协调者回合结束都不能单独证明相关写入停止。
4. 若之后明确确认相关写入停止：先核对实际产物基线，再按 Roadmap 修订为新方案版本，更新排序规则、依赖与 AC-sort，保留文案有效范围。新版就绪且可访问后，以同轮后续消息续接现有协调者；新排序产物须独立验收，并核对最终集成版本。

② 拟更新的记录／状态

- 最新授权不变：更新时间降序；文案继续。
- 排序：暂停已请求，实际仍运行；新版交接仍受阻。
- P1/V1 的 PASS：保留为历史证据，不能满足当前 AC-sort；当前需求未验收。
- worker-B：继续运行；整体未完成。
- 停止确认后记录最后写入版本；新版形成前仍不标记排序可执行。

③ 给用户的简短回复

“模拟反馈表明，停止要求已送达，但排序写入仍在进行。拟继续协调确认停止，空结果文案保持推进。旧版验收通过不能证明新的排序要求已满足；停止确认后，将修订方案并安排实现与独立验收。”

④ 缺失证据／待确认事项

拟读取：worker-A 与关联命令的停止证据、最后写入内容及版本、受影响任务与验收映射。明确停写后，还需当前代码和测试依据、新方案版本及后续独立验收证据；等待主持人提供，不实际访问目标。
````

#### B04-A

````text
以下均为草案，未执行发送或状态写入。

① 有序动作草案

1. 依据题设，将 R01 的通过范围限定为 P2 下的 T01/AC01、产物 V1，引用 `/fixture/evidence/ac01-v1`。整体仍剩 REQ02、REQ03。
2. R01 已停止且无其他写入，T02 已就绪；登记 R02 交接意图，拟续接 `exec-orders-01`，保留已有模型设置，无需重做规划或逐轮请示。拟发送：

> R02：依据 P2，在 `/fixture/wt-orders` 承接 V1，完成 T02/AC02。由唯一写入者修改 pagination handler，在翻页回调保留当前筛选值；验证 paid 筛选后翻页仍只显示 paid。遵循既有执行协议，完成自测及独立验收，检查此次变更对 AC01 的影响，必要时重新验收。更新原执行记录，回报轮次、实际工作区、产物版本、逐项 AC 与证据、未验证项、阻塞和在途工作。整体终点仍为集成到 `/fixture/luna-shop` 的 main 并验证。

3. 拟在确认发送后保存回执，按不超过 60 秒的有界等待回收结果。T02 通过后继续处理 T03；最终集成及整体验收通过前不宣布目标完成。

② 拟更新的记录/状态

- R01：已处理；T01/AC01：PASS，仅绑定 V1。
- R02：待派发；T02：TODO、依赖已满足，不提前记 RUNNING/PASS。
- T03/AC03：待 T02；整体目标：未完成。
- 继续沿用既有任务、工作区及记录，不新增任务或后台自动化。

③ 给用户的简短回复

“筛选阶段已通过。下一步将继续分页保持筛选及独立验收，随后推进原项目 main 的集成验证；无需您再次确认。”

④ 缺失证据或待确认事项

题设已提供下一轮所需条件，无新增用户决策项。发送回执、T02 产物与验收、最终 main 的集成及整体验收证据尚未产生，均不得预填。
````

#### B04-B

````text
以下均为草案，未实际派发、集成或更新记录。

① 有序动作草案

1. 按新增反馈回收 R02：T02/AC02 在 V2 通过独立验收，AC01 证据仍适用，协调者已停止写入。确认交付尚未完成：原项目 main 仍为 V0，REQ03/AC03 未满足。
2. 登记 R03 交接意图，拟续接 `exec-orders-01`，保留已有模型与执行安排。拟发送：

> R03：依据 P2 执行 T03/AC03。输入为 `/fixture/wt-orders` 已验收产物 V2，目标为 `/fixture/luna-shop` 的 main，当前基线 V0。核对实际差异与基线，将授权成果集成到原项目 main，保留无关内容。冻结待验收版本，由独立验收者在原项目执行筛选与分页组合验证：paid 筛选后翻页仍仅显示 paid。核对最终版本的全部必需 AC，复用仍有效证据，补齐受集成影响的验证。回报轮次、实际集成版本、差异、逐项验收与证据、未验证项、阻塞及在途工作，更新原执行记录。

3. 拟确认送达后保存回执并有界等待；最终版本满足全部必需验收、无影响完成判定的在途写入后，才报告整体交付。

② 拟更新的记录/状态

- R02：已处理；T02/AC02：PASS@V2；T01/AC01：证据仍适用。
- R03：待派发；T03：TODO、依赖已满足；AC03：未验证。
- worktree：阶段成果通过；原项目 main：V0、尚未集成。
- 整体：未完成；下一责任人：原执行协调者。

③ 给用户的简短回复

“筛选与分页保持筛选已在 worktree 通过验收。下一步将继续集成到原项目 main，并完成组合验证；整体交付仍待这一步通过。”

④ 缺失证据或待确认事项

无需新增授权。尚缺实际集成版本、AC03 及最终状态的必要验收证据；不能以 V2 的阶段通过替代原项目验收。
````

#### B05-A

````text
① 有序动作草案

1. 自动派发条件已确认无法满足：协调者没有可并行的实际工作，名额充足不改变此限制。拟采用用户已接受的手动接力，不创建新任务。
2. 按 [Roadmap](/Users/kaka/kDevelop/Skills/kCrazyLuna/skills/luna-roadmap/SKILL.md) 的“必要派发条件经调整仍无法满足时，为受影响环节提供手动会话 Prompt”，交付以下内容。用户新开独立验收会话，选择建议配置并粘贴；新会话先核实前提，再验收。

> 交接编号：B05-A01；从原执行记录核对并回报所属轮次。
> 你担任 T01 的独立验收者，须未参与制作，在独立上下文中验收，不修改验收对象。
> 权威方案：/fixture/plan-orders-P3.md；执行记录：/fixture/run-orders.md；实际产物：/fixture/wt-orders，冻结版本 V3。
> 建议配置 gpt-5.6-luna / medium。先核实实际模型、档位及读取文件、检查差异、执行命令的工具能力；Prompt 不会切换配置。无法满足或核实必要条件时回报阻塞，不静默换型、降档。
> 读取项目指令、方案、权威需求及记录，核对需求覆盖、任务范围和无在途写入。用记录中的 V3 摘要或快照核对实际内容；存在未提交改动时不能仅比 HEAD。不一致或缺版本依据则停止验收并回报。
> 独立检查实际差异，确认授权范围及原有无关改动得到保留。按方案已核实的环境，在 /fixture/wt-orders 执行 `npm test -- orders-filter`。
> 逐项验收：AC01 选择 paid 仅显示 paid；AC02 筛选空结果展示提示；AC03 切回全部恢复所有订单。报告对应证据，区分静态检查、测试与实际运行，未执行项标为未验证。
> 回报轮次、交接编号、T01、实际配置与工作区、V3 标识、实际差异依据、命令及结果、逐项 AC 结论、未验证项、阻塞和在途工作。结束前再次核对版本一致。将结果交回原协调者，由其更新原执行记录；发现缺陷交回返工。

② 拟更新的记录／状态

- 原执行记录：保留制作、自测已完成的事实；T01 拟记 `BLOCKED`，原因是独立验收尚未启动、手动会话必要条件待核实。条件满足且开始验收后转 `VERIFYING`。
- 既有 Keeper 记录：登记 B05-A01 交接草案、P3／V3、剩余范围仅最终独立验收，状态“待用户手动派发”；实际验收模型、档位和会话标识均为未知。
- 未安排自动派发、自动回收或后台接续。收到人工回传后核对归属、证据及最终版本；全部必需 AC 满足才可拟记 `PASS`。本次不实际写入任何记录。

③ 给用户的简短回复

制作与自测已完成，最终独立验收仍未完成。当前宿主无法满足验收子 Agent 的派发条件，请新开独立会话，建议选择 gpt-5.6-luna／medium，并粘贴上面的交接 Prompt。验收结果请带回原协调者更新执行记录，再回到 Keeper 核对最终完成条件。

④ 缺失证据或待确认事项

手动会话的实际模型、档位、工具能力与独立性；V3 的可核对标识及前后版本一致性；实际差异和 AC01—AC03 的独立验证证据。上述内容由手动会话核实并回传，不补造结果，也不把已接受人工接力视为已授权自动创建任务。
````

## 操作审计与文件保护

演练主持人实际使用 collaboration.spawn_agent 创建 5 个只读演练 Agent，以 followup_task 提供 3 份预定义 B 阶段反馈；每个场景独立启动，未复用其他场景上下文。回收结束后另行索取只读操作自述，不提供新业务输入，不计作行为重跑。各执行者仅获准读取公共说明中的 4 个包文件；下表工具轨迹来自执行者自述，未导出宿主逐调用审计日志。

| 场景 | 执行者自述的真实工具操作 |
| --- | --- |
| B01 | functions.exec 编排，共 6 次 exec_command/cat；4 文件首读，因输出截断补读 Roadmap 正文及 Keeper 运行参考。 |
| B02 | 2 次 functions.exec、6 次 exec_command/cat；同上 4 个指定文件，补读截断内容；未调用真实 wait_threads。 |
| B03 | 2 次 functions.exec、3 次 exec_command；一次 cat 合读 4 文件，再分别补读运行参考及 Roadmap 正文。 |
| B04 | 2 次 functions.exec、6 次 exec_command/cat；同上 4 个指定文件，补读截断内容。 |
| B05 | 2 次 functions.exec、6 次 exec_command/cat；4 个指定文件首读，再补读运行参考及 Roadmap 正文。 |

已回收的操作自述均声明没有真实业务写入、任务派发、消息发送工具调用、自动化或网络调用。真实 Agent 调度只用于本次离线演练；没有对场景中的模拟项目和任务进行真实操作。实际运行模型/推理档位未获宿主回报，记为未知；场景中的 Terra/Luna 型号是测试输入或拟选配置，不作为实际演练模型证明。

2026-09-20 16:45 的内容核对：受保护的 15 个文件与开工 SHA-256 逐项完全相同，包括全部 Skill 正文、模型与运行 references、UI、校验脚本、README 和 AGENTS.md。本轮只新增验收说明与本记录；没有 Skill 修复、依赖、模型缓存、测试脚本或日常调用入口改动。

## 完成与限制

- 本次 5 个场景未发现需修改 Skill 的失败行为，故未提出或实施 Skill 修复。
- 5 个场景各运行一次，共 8 个观察阶段；不代表统计稳定性、所有边界或跨模型表现。
- 未执行真实 send_message_to_thread、wait_threads、任务暂停、业务代码实现、集成或人工验收；场景反馈均为预制材料。
- 只读操作自述与文件摘要是本轮可得证据，不替代宿主全量操作审计；真实模型、工具授权与运行效果仍需单独验证。
- 静态包检查和 28 项既有回归通过；其证据范围与上述行为演练分别计算。
- 两份新增文档的代码围栏与本地链接检查通过；交付时已清理发现的文末多余空行。
