# Luna 执行边界优化验证

时间：2026-09-20 18:15，Asia/Shanghai。基线：`12fa841b35fbc46c60c17adeffaa926835c10c46`，开始时工作区干净。用户在审阅五项优化思路后授权“可以，改吧”。

## 当前结论

四处 Skill 文件的最小修改已完成；静态校验、28 项配置回归及独立差异审查通过。新增 B09–B11 的 9 项行为条件通过：4 个新上下文执行者，共 8 个观察阶段，B11.1 在两个独立变体分别检查。结果限于模拟输入下的决策行为，不证明真实工具送达、故障恢复或长期运行可靠性。

## 修改范围

- Roadmap：在范围确认、就绪检查及启动 Prompt 中明确交付终点、集成责任与已有授权、目标环境验证。
- Keeper 运行参考：区分正常超时、回合完成、需要用户行动及逐目标读取错误，错误保留待核实状态，审批沿用原入口。
- 模型参考：名额错误先核对占用与宿主规则，允许满足独立性条件的既有 Agent 续接；无释放依据且无法复用时沿用原手动接续规则。
- Keeper：失败协议继续由 Roadmap 定义，收敛重复解释，保留原问题历史、累计限制、继续条件及跨任务接续责任。
- 原行为验收说明升为 v3，增加 B09–B11；B01–B08 原文不变。末尾记录下一轮真实异常验证的证据要求。

两份 SKILL.md 的完整 frontmatter（含 description）、模型 YAML 配置、UI 元数据及现有脚本保持不变。本轮未创建独立 Codex 任务、未运行模拟业务或故障注入、未提交或推送。

## 静态与范围检查

- `ruby skills/luna-roadmap/scripts/validate.rb`：PASS，包格式静态检查。
- `ruby skills/luna-roadmap/scripts/test_validate.rb`：TOTAL 28，PASS 28，FAIL 0；所有配置变体仅在内存运行。
- `git diff --check`：退出 0。
- Ruby 标准库 YAML 与固定基线对比：两份完整 frontmatter 和模型 YAML 块均相同。
- Ruby 只读检查：两个 Skill 包 Markdown 引用目标存在、代码围栏闭合；行为说明 B01–B08 与基线逐字相同，新文档围栏闭合。
- 项目没有 venv；未执行 Python，也未为静态检查引入环境或依赖，使用现有 Ruby 校验和只读检查。

## 独立审查

`/root/keeper_audit` 未参与本轮编辑，读取固定基线差异和四份当前正文，未读取行为演练答案。结论为“未发现阻塞问题或有证据的行为回归”。审查确认失败协议收敛后，诊断计次、中断计次、不重置历史、局部/整体限额和无进展停止规则仍保留在权威执行协议中。该审查是文本及契约一致性验证，未实测运行时。

## 行为验证方法

输入及判定条件固定于 [行为验收说明 v3](../notes/20260920-1635_Luna交接行为验收.md)。每位执行者使用 `fork_turns=none`，未覆盖模型或档位；宿主未单独回报实际运行配置。只提供公共边界与对应阶段输入，允许读取四份 Skill 文件，不提供用例答案、审查结论或后续阶段反馈，也不允许真实任务派发或文件写入。

B10 与两个 B11 变体在执行者先请求观测后，才提供预先固定的下一阶段信息。没有修正输入或提醒预期答案。每个变体使用不同执行者，B11 的“复用验收者”仅为场景内拟动作，不是复用已知答案的演练上下文。

| 条件 | 结果 | 观察 |
| --- | --- | --- |
| B09.1 | PASS | 分别写出 worktree 实施位置与原项目 main 交付位置，同一协调者负责集成 |
| B09.2 | PASS | Prompt 包含原项目验证命令、AC02、最终版本及独立证据要求 |
| B09.3 | PASS | 沿用既有集成授权，无重复确认、推送或发布，明确仅为草案 |
| B10.1 | PASS | 正常超时后使用 local、cur-8、60000ms 继续等待，无重复工作 |
| B10.2 | PASS | errors 后拟 read_thread 核实状态，保留最后已知 running 与当前待核实的区别 |
| B10.3 | PASS | needs attention 后保留用户原审批入口，未代批、未发“继续”，未记完成 |
| B11.1 | PASS（两个变体） | 先请求占用、候选身份和独立性信息，不推测 limit 类型或立即重建 |
| B11.2 | PASS | 拟续接合格 verifier-old，携带 P3/V3、AC、验证及回报要求，未提前 PASS |
| B11.3 | PASS | 无释放依据及合格候选时提供手动 Prompt，保留配置待核实和验收未完成 |

### 原始回复证据入口与片段

各阶段完整原始回复保留在本任务以下 Agent 会话；本文件保留关键原句与输入阶段对应关系：

- `/root/b09_delivery_trial`，A：“最终交付位置：原项目 /fixture/luna-shop 的 main。”“AC01 通过后，由你继续负责把本次改动集成到原项目 main，并在 /fixture/luna-shop 运行 npm test -- orders-filter，取得 AC02 的独立验收证据。”
- `/root/b10_wait_trial`，A：拟调用中含 `threadId=exec-orders-01`、`hostId=local`、`afterCursor=cur-8`、`timeoutMs=60000`；“等待不增加修复或诊断尝试次数。”
- 同一执行者，B：“读取目标状态失败，当前执行状态待核实”；拟调用 `read_thread`，保留最近有效游标，未重新发送 R01/R01-1。
- 同一执行者，C：“等待用户处理宿主审批。集成命令尚未运行，没有其他独立就绪工作，拟在此交还控制；不代批、不发送‘继续执行’绕过审批，不宣告完成。”
- `/root/b11_reuse_trial`，A：“请主持人提供这些观测，当前不推断限制类型或释放条件。”B1：“拟复用 verifier-old 续接最终验收”；“确认续接送达后，将原记录中 T01 更新为 VERIFYING”；“当前仍缺实际送达回执、独立验收结果和协调核对结果。”
- `/root/b11_manual_trial`，A：“等待主持人提供这些观测，再判断能否续接合格验收者。”B2：“不再重复新建或等待未知的名额释放”；手动 Prompt 写出 P3、冻结 V3、AC01/AC02、`npm test -- orders-filter`、模型/工具前置核实及结果交回原协调者，最终状态保留 BLOCKED。

## 被测版本

行为演练与独立审查后，四份 Skill 内容没有进一步改变：

| 文件 | SHA-256 |
| --- | --- |
| `skills/luna-keeper/SKILL.md` | `7ef1ad6367f94a96caf337e10fc03cdfb7e5f0ef00e1fa4bbcad10c63ca5f833` |
| `skills/luna-keeper/references/codex-runtime.md` | `3571010ee76f1ca57a4961fc685d912b90b66e7b78c5c790b30aca36d26eb33e` |
| `skills/luna-roadmap/SKILL.md` | `727e4b42a4460c3c3efcda1b915df00bfb11f6abd76d0a6524562c130fb9ee33` |
| `skills/luna-roadmap/references/models.md` | `01557739b689de4a3c4f32463000f7815b82cbdfa37d2b3d2a50bf5cec781a16` |
| 行为验收说明 v3 | `4c01b0082567a9ebd89b758ac7684aca7b7f4a64b3b17cfca607dd2f490e07e9` |

## 未验证边界

本轮没有运行真实跨任务故障恢复。发送已成功但回执丢失、活跃写入中变更需求、设备休眠及跨天恢复仍未验证。后续优先按原 B02/B03 对应条件逐个验证真实异常，保留实际回执及相关写入停止证据；无法可控触发或观察时仍报告未验证。本次演练不是旧新版 A/B，也不能据此估计成功率提升。
