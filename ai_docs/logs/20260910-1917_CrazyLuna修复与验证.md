# CrazyLuna 修复与验证

时间：2026-09-10 19:17，Asia/Shanghai。

## 当前需求与修改

用户要求默认 Luna 中度推理；明确指定更强模型时采用用户指定；入口去掉“大型任务”“详细修改方案”；补齐审计中指出的执行验证缺口。

本轮修改 `skills/crazy-luna/SKILL.md` 的模型默认值、模型覆盖传递及启动模板，简化发现描述；修改 `skills/crazy-luna/agents/openai.yaml` 的入口文案。保留按实际复杂度规划、独立验收与版本绑定规则。

没有新增依赖、初始化 Git、创建 Goal 或修改历史验证目录。当前全局安装为软链接 `/Users/kaka/.codex/skills/crazy-luna`，指向本项目运行包；本轮无需另行安装或同步文件。

## 验证范围与基线

- 当前 Skill SHA-256：`774fcde6343e812ef2815325f6782d3683dde42b4dfcda1600cc21898f10a36f`。
- 当前 UI 配置 SHA-256：`d4e031c4e9ff0aa4d8f9956585e8803937dc5acd332556eeebf99844fecd6533`。
- 复跑创建记录中的 Ruby 格式检查：退出码 0，YAML、frontmatter、命名、UI 元数据、围栏、脚手架及空白均 PASS，当前入口 127 行。原版 Python quick_validate.py 未执行。
- 新样例项目：`/private/tmp/kcrazy-luna-verify-nZrsmY/catalog`，复制旧样例五个原始业务文件，原目录不变。
- 权威方案：该样例 `ai_docs/notes/20260910-1917_CrazyLuna执行验证方案.md`，v1.0，包含原始需求、确定设计、T01/T02、AC01–AC16 和已填写的启动 Prompt。
- Node `v25.9.0`，npm `11.12.1`；样例基线 `npm test` 退出码 0，2/2 通过。
- 本文由主协调者单独维护，是本轮样例唯一执行记录。临时样例不是运行包依赖。

## 执行记录

| 任务 | 负责人 / 请求模型 | 状态 | 产物版本 / 证据 | 阻塞原因 / 下一步 |
| --- | --- | --- | --- | --- |
| T01 标签规范化 | /root/luna_execute_validation / gpt-5.6-luna medium | PASS | /root/luna_verify_validation 独立验收 AC01–AC08、AC14–AC16 通过 | 无 |
| T02 分币求和及集成 | 复用 T01 执行者 / gpt-5.6-luna medium | PASS | 独立验收 AC09–AC16 通过；冻结哈希一致 | 无 |

两项任务经历 RUNNING、VERIFYING，再经独立验收置为 PASS。请求模型与宿主实际模型回报分别记录。

本次执行派发显式使用 `model: gpt-5.6-luna`、`reasoning_effort: medium`、`fork_turns: none`。宿主只返回 Agent 任务标识，没有返回实际运行模型字段，故实际模型未获独立确认。

### 执行者回报与冻结版本

执行者已实际新增两个函数、公共导出及 AC01–AC13 相关测试，保留旧测试。回报四项 `node --check` 和 `npm test` 均退出码 0，15/15 测试通过，阻塞为无。主协调者随后亲自读取四个实际文件并核对五文件哈希，结果与执行回报一致；这一步不替代独立验收。

| 文件 | 待验收 SHA-256 |
| --- | --- |
| src/index.mjs | `418c4c2024b0689864093d3037b2c016ed3d6f4c400d0b2d6b1e44c253af54b9` |
| src/labels.mjs | `e150949e284a80ef2f911945e1377b91605185f8dd47c6a37de76a5fd5db36db` |
| src/prices.mjs | `81238fffbd863aadc6296004f0bd2996f72e1dfe8031846b1353b86cc0e61e39` |
| package.json | `4a3a0ea61f3b7b42e00ae53b1fb6c31ed524e756a87bc4dd205524abe2693727` |
| test/catalog.test.mjs | `62bb4e9087b4ebedf7769c98287d2103f358cecf753aa9f69dba01438595f0ec` |

方案 v1.0 SHA-256：`d5e8b1553ec99b0da4cd06a829c48f94955becf2bd94a0486072ad55e7b6cfec`。执行者已明确停止写入，本轮不再派发并行业务修改。

## 独立验收结果

验收 Agent：`/root/luna_verify_validation`，另行以 `gpt-5.6-luna`、`medium`、`fork_turns: none` 派发，未参与实现。宿主同样只返回任务标识，未提供实际模型字段。

验收者读取完整方案、五个实际文件及原始基线，确认 R01–R14 对应 T01/T02 和 AC01–AC16，无原始需求遗漏；业务差异仅在四个允许文件，旧函数体和测试保留，package.json 未变。以下命令由执行者及独立验收者分别实际运行，工作目录均为 `/private/tmp/kcrazy-luna-verify-nZrsmY/catalog`：

```sh
node --check src/labels.mjs
node --check src/prices.mjs
node --check src/index.mjs
node --check test/catalog.test.mjs
npm test
```

两次验证均为四项语法检查退出码 0；`npm test` 退出码 0，15/15 通过，0 失败。

| AC | 结果 | 实际证据 |
| --- | --- | --- |
| AC01 | PASS | 现有测试文件新增空数组与普通标签数组断言 |
| AC02 | PASS | 首尾空白、制表与换行 trim 断言 |
| AC03 | PASS | 全空与混合空标签过滤断言 |
| AC04 | PASS | 大小写规范化断言 |
| AC05 | PASS | 规范化后去重并保留首次顺序的断言 |
| AC06 | PASS | 成功及异常路径的冻结输入保持断言；独立探针补足结果数组引用独立性 |
| AC07 | PASS | 非数组和类数组逐项 TypeError 断言 |
| AC08 | PASS | 非字符串元素及稀疏数组 TypeError 断言 |
| AC09 | PASS | 正常求和和三个安全整数上界断言 |
| AC10 | PASS | 空数组返回数值 0 的断言 |
| AC11 | PASS | 非数组和类数组 RangeError 断言 |
| AC12 | PASS | 非法数字、非数字及稀疏数组 RangeError 断言 |
| AC13 | PASS | 直接与累积溢出 RangeError 断言，并检查相加前的上界判断 |
| AC14 | PASS | 全部测试经公共入口导入，新旧四个函数均可调用 |
| AC15 | PASS | 原有两条测试通过，旧函数体与原始基线逐字一致 |
| AC16 | PASS | 完整文件清单、四文件差异、package.json 哈希、语法检查与单元测试证据 |

验收者的正确版本独立行为探针如下，实际退出码 0，输出 `independent behavior assertions: PASS`：

```sh
node --input-type=module -e "import assert from 'node:assert/strict'; import {normalizeLabels,sumCents} from './src/index.mjs'; const labels=Object.freeze([' A ','','a','B']); const out=normalizeLabels(labels); assert.notStrictEqual(out, labels); assert.deepEqual(out,['a','b']); assert.deepEqual(labels,[' A ','','a','B']); assert.throws(()=>normalizeLabels(new Array(1)),TypeError); assert.equal(sumCents([-0]),0); assert.throws(()=>sumCents([Number.MAX_SAFE_INTEGER,1]),RangeError); assert.equal(sumCents([Number.MAX_SAFE_INTEGER-1,1]),Number.MAX_SAFE_INTEGER); console.log('independent behavior assertions: PASS');"
```

现有样例测试没有单独写 `assert.notStrictEqual`，本次由上述独立行为验证补足 AC06 的引用检查。验收者初次探针误把 `-0` 直接作为顶层参数传入，退出码 1；更正为契约要求的数组 `[-0]` 后通过，没有修改实现或降低验收要求。

## Skill 修改的独立静态核对

验收者逐项核对正文、启动模板与 UI 文案，以下三个情景的规则一致：

| 情景 | 模型与推理强度 |
| --- | --- |
| 用户无覆盖指定 | 全部任务子 Agent 为 gpt-5.6-luna / medium |
| 用户只为验收角色指定更强模型 | 验收采用指定模型 / medium；其他角色保持默认 |
| 用户同时指定模型和推理强度 | 对应角色采用指定值；其他角色保持默认 |

上述情景是指令静态核对，没有为验证覆盖而实际派发更强模型。规划阶段的强模型描述、正文的重复工作分类仍服务原有工作流，任务子 Agent 的默认值及入口文案已按本轮需求修改。

## 最终范围与限制

主协调者对实际字节的核对确认：运行包仅有本轮两文件必要修改，旧创建日志未改，旧样例五文件保持原样，新的样例只修改四个允许文件；没有新增依赖、额外测试文件或业务实现路径。全局软链接指向当前修订文件。

本轮补齐了小样例的规划交接、实际实现、自测、独立验收和最终版本核对。证据绑定本记录中的 Skill、配置、方案和样例哈希；不代表真实大型项目、长期 Goal 或端到端成本收益已获验证。模型请求成功不替代宿主实际模型证明；原版 Python quick_validate.py 没有运行。
