# Luna 真实跨任务执行验收方案

版本：v1。创建时间：2026-09-20 16:55，Asia/Shanghai。

## 目标、范围与当前就绪状态

通过一个可确定验收结果的 Ruby 小任务，真实验证 Luna Keeper 与 Luna Roadmap 的原生任务创建、worktree 输入访问、子 Agent 实施与独立验收、结果等待、消息续接、需求版本变化和最终产物交付。两轮实现后再核对集成产物，不能将阶段通过当作整体完成。

Skill 正文、配置、现有校验脚本、README 和业务文件保持原样。仅新增本方案、本次记录，以及 `tmp_luna/live-20260920-1655/` 内的固定数据、标准库验收脚本和后续集成产物。执行者在独立 worktree 的同名测试目录内实现；执行记录放在该 worktree 的 ai_docs/logs/。不新增依赖、测试框架或服务，不提交、推送、合并 Git 或发布，不创建后台自动化。

本轮是一次有界的真实主路径验证，不故障注入网络超时，不强制中断活跃写入，不模拟设备休眠，不宣称已验证长时间稳定性。出现无法解除的权限/接口阻塞时保留实际证据；每轮最多一次有依据的纠正重试，仍失败则停止该轮并报告，不修改被测 Skill。

预检：当前宿主提供 create_thread、read_thread、wait_threads 和 send_message_to_thread；目标项目 S4.CrazyLuna 的 projectId 为 `db1450bd-4fa9-4561-8a65-5a4d088e7b29`，isGitRepository=true，hostId=local。拟使用默认分支创建 worktree，不指定 startingState。原项目未提交的 Skill 和本轮输入通过明确的原项目绝对路径读取，不假定它们存在于新 worktree。

创建权限：用户已回复“确认实跑”，明确批准新建 1 个隔离 worktree 任务及下列角色模型配置。按此授权执行，不重复确认；不得复用无关任务。

## 文件与业务契约

原项目固定输入：`/Users/kaka/kDevelop/Skills/kCrazyLuna/tmp_luna/live-20260920-1655/orders.json`。
验收入口：`/Users/kaka/kDevelop/Skills/kCrazyLuna/tmp_luna/live-20260920-1655/verify.rb`。
执行实现：`<实际 worktree>/tmp_luna/live-20260920-1655/filter_orders.rb`。
最终集成位置：`/Users/kaka/kDevelop/Skills/kCrazyLuna/tmp_luna/live-20260920-1655/integrated/filter_orders.rb`。
Keeper 记录：`/Users/kaka/kDevelop/Skills/kCrazyLuna/ai_docs/logs/20260920-1655_Luna真实执行验证记录.md`。
执行协调记录：`<实际 worktree>/ai_docs/logs/20260920-1655_Luna真实执行协调记录.md`。

CLI：`ruby filter_orders.rb <orders.json> <all|pending|paid|cancelled>`。输入是 JSON 数组，包含 id、status、created_at、updated_at；输出筛选后的完整对象数组 JSON 到 stdout。all 不过滤；cancelled 在本固定输入中返回空数组。不修改输入，不扩展非法参数、日期解析或服务端功能。

| 需求版本 | all 顺序 | paid 顺序 | pending | cancelled |
| --- | --- | --- | --- | --- |
| P1：created_at 数值降序 | o3,o2,o4,o1 | o2,o4,o1 | o3 | 空数组 |
| P2：updated_at 数值降序 | o1,o4,o2,o3 | o1,o4,o2 | o3 | 空数组 |

验证脚本逐项比较完整输出与固定预期，记录子进程退出码、错误输出、实现 SHA-256 与输入前后摘要。退出 0 表示全部匹配；1 表示验收不满足；2 表示调用或文件前提错误。该脚本只负责产品行为，不代替跨任务过程证据。

## 角色与可执行条件

- Keeper 与规划沿用当前会话配置。
- 已确认的新执行协调者：gpt-5.6-terra / medium，以 create_thread 的 model/thinking 参数落实。
- 实施与独立验收子 Agent：gpt-5.6-luna / medium；需要模型覆盖时以 fork_turns=none 和完整参数派发，重新核对目标执行任务的实际工具约束。
- 实施 Agent 运行期间，协调者检查输入、基线和记录入口；验收 Agent 运行期间，协调者做范围保护核对和证据索引。并发上限以实际剩余名额为准，只派发满足宿主条件的独立工作。
- 若预定派发方式无法成立，不虚构并行工作、不静默降级或更换模型；报告具体限制及已完成部分。实际模型/档位未被宿主回报时记未知，不能把请求参数当实际运行证明。
- 子 Agent 向执行协调者回报；协调者在真实原生执行任务中回报 Keeper。保持输入/验收脚本只读，冻结实现后才验收。

## 真实执行步骤与验收

| ID | 步骤 | 验收与证据 |
| --- | --- | --- |
| LIVE01 | 创建专用 worktree 任务，发送 R01-1 | 保存创建返回、真实 threadId/hostId（不能以 clientThreadId 替代）、实际工作区与基线；确认能读取原项目指定输入与 Skill |
| LIVE02 | R01/P1 实现及独立验收 | 实际子 Agent 派发、独立身份、P1 四项输出 PASS、实现 V1 摘要、无影响后续写入的在途工作 |
| LIVE03 | Keeper 显式等待并回收 R01 | 使用不超过 60 秒的等待和真实游标；核对消息编号、产物及证据，不重复派发；本轮通过时整体仍未完成 |
| LIVE04 | 向同一任务发送 R02-1，改为 P2 | 确认 R01 停止相关写入后发送；同一工作区更新排序、独立验收 P2 四项 PASS，记录 V2；P1 旧证据不用于 P2 完成判定 |
| LIVE05 | 最终产物集成与独立核对 | Keeper 将已验收 V2 复制到原项目专用 integrated 目录，核对源/目标摘要并跑 P2；用 P1 验收该版本应失败，保留“预期失败”证据；R03-1 让原协调者安排独立核对集成产物并回传证据 |
| LIVE06 | 整体交付与范围保护 | 最终 P2、独立验收、消息归属与版本均一致；原有保护文件摘要不变；区分已验证与未验证项，不自动清理 worktree 或归档任务 |

“集成”仅指专用测试目录中的产物传递和最终验证，不是把测试代码合并进原项目业务或 Git main。

每次发消息前记录轮次/消息、确切 Prompt 与待发送意图，成功后记录回执。发送结果不明时查询原任务，不能盲目重试；等待超时只表明未收到新结果。按当前 Skill 的恢复规则处理真实发生的异常，不主动制造异常后宣称全面覆盖。

R02 的确切请求：只把默认排序从 created_at 降序改为 updated_at 降序，筛选及完整对象输出保持一致；范围仍是上述实现文件与执行记录，按 P2 验收，完整回报后停止等待 R03。
R03 的确切请求：仅读取 integrated 下的冻结产物和原输入，独立核对其摘要与 V2 一致、执行 P2 验收；不得修改集成产物。协调者同时核对 worktree 最终范围、子 Agent 结束状态和证据索引，回报整体证据。

## 待创建时发送的 R01 Prompt

```text
请执行 Luna 真实跨任务验证的 R01 / R01-1，暂不进入下一轮。
权威方案：/Users/kaka/kDevelop/Skills/kCrazyLuna/ai_docs/notes/20260920-1655_Luna真实执行验收方案.md，v1。
输入：/Users/kaka/kDevelop/Skills/kCrazyLuna/tmp_luna/live-20260920-1655/orders.json；验收脚本：/Users/kaka/kDevelop/Skills/kCrazyLuna/tmp_luna/live-20260920-1655/verify.rb。
当前任务是专用测试协调者，请先读取上述方案、项目指令，以及原项目以下当前 Skill：
/Users/kaka/kDevelop/Skills/kCrazyLuna/skills/luna-roadmap/SKILL.md
/Users/kaka/kDevelop/Skills/kCrazyLuna/skills/luna-roadmap/references/models.md
采用实际分配的 worktree，先报告其真实绝对路径和 Git 基线；原项目的 Skill、输入、验收脚本和方案仅供读取。
允许写入：当前 worktree 的 tmp_luna/live-20260920-1655/filter_orders.rb，以及 ai_docs/logs/20260920-1655_Luna真实执行协调记录.md。不得修改其他项目文件、Skill、输入或验收脚本，不提交、推送、合并或创建自动化。
R01：Ruby CLI 从第一个参数读订单 JSON 数组，第二个参数为 all/pending/paid/cancelled；筛选后按 created_at 数值降序输出完整订单对象数组 JSON 到 stdout。使用标准库，不安装依赖。
实施和独立验收角色按方案使用 gpt-5.6-luna / medium，fork_turns=none，实际派发前核对完整工具条件。验证：
ruby /Users/kaka/kDevelop/Skills/kCrazyLuna/tmp_luna/live-20260920-1655/verify.rb <实际实现绝对路径> /Users/kaka/kDevelop/Skills/kCrazyLuna/tmp_luna/live-20260920-1655/orders.json P1
独立验收由未参与实现的 Agent 完成，按权威方案要求保存身份、真实派发参数、逐项结果、命令、实现 SHA-256 及未验证项；不以自测代替验收。必要派发条件不满足时报告具体阻塞，不能虚构并行工作。
你在实施 Agent 工作期间完成输入/基线可访问性与本轮执行记录；在验收 Agent 工作期间完成原项目和 worktree 的范围保护核对与本轮证据索引。这些是本次验收必需的独立工作，不改正在验收的实现文件。
回报 R01/R01-1、P1、实际工作区、实现位置与 SHA-256、子 Agent 标识与配置、逐项验收证据及所有在途任务/命令状态。R01 交接范围完成后停止本轮并回报，等待 Keeper 发送 R02；不能将 R01 通过报告为整体目标完成。
```

## 验证记录

实际操作、失败和未验证项统一写入本次日志。只有原生创建/发送/等待和子 Agent 执行真的发生，才将对应 LIVE 项标为 PASS；准备完成或语法检查不能计为真实跨任务通过。
