# 订单筛选方案 v1

A 根目录 /Users/kaka/.codex/worktrees/b2ce/kCrazyLuna，基线 b4f8d366cd2dde0b50372f11d857c553226a5ad1，detached HEAD，初始工作区干净。项目 S4.CrazyLuna，B 新建隔离 worktree，报告实际根目录。

目标：Ruby 标准库 CLI `ruby filter_orders.rb <orders.json> <all|pending|paid|cancelled>`，JSON 数组按状态筛选，all 不过滤，输出完整对象数组 JSON，输入不变。P1 created_at 数值降序；P1 独立验收回报 A 后，A 再派发 P2 改为 updated_at 数值降序，保留筛选和完整对象。

只读输入 /Users/kaka/kDevelop/Skills/kCrazyLuna/tmp_luna/live-20260920-1655/orders.json；只读验收同目录 verify.rb，调用 `ruby verify.rb <实现> <输入> <P1|P2>`。已直接阅读，两阶段各覆盖四种状态、完整对象和输入 hash。本机 Ruby 2.6.10 可用。数值键 sort_by 后 reverse；仅需 json 库，不投影字段。

B 唯一实现：B根/tmp_luna/entry-20260920-1901/filter_orders.rb。A 负责复制至最终终点 /Users/kaka/.codex/worktrees/b2ce/kCrazyLuna/tmp_luna/entry-20260920-1901/integrated/filter_orders.rb，写入前检查不存在。记录各自在 ai_docs/logs/YYYYMMDD-HHMM_主题.md，方案在 ai_docs/notes/。固定输入、验收、已有实现、README、Skill 及其余原项目文件只读。不读取历史方案、日志、其他对话或历史实现。无新增依赖、框架、服务、自动化、Git 提交推送合并、归档或清理。

T01/AC01/R01：实施者制作 P1，自测 ruby -c 和现有 P1；独立验收者核源码、冻结 SHA256、运行 P1，四项 PASS、对象相等、fixture_unchanged=true。回报 A 并停止写入等待 R02。
T02/AC02/R02：依赖 A 回收 AC01，实施者仅改排序键；独立验收运行 P2，记录 SHA256 与原始输出，核对筛选及完整对象契约。
T03/AC03/R03：A 回收 AC02 后复制最终文件，B 派发未参与制作的独立验收者在 A 最终路径运行 ruby -c、P2，核对 A/B hash 一致及输入不变。B 同时核对本轮全部改动相对基线、需求覆盖与执行记录一致性，A 核对自身最终差异。P1 是中间成果，三个任务全通过才完成。

模型：A 沿用用户指定 gpt-6-astra/medium；B create_thread model=gpt-5.6-terra thinking=medium；实施者和独立验收者 spawn_agent model=gpt-5.6-luna reasoning_effort=medium fork_turns=none。接口声明支持组合；B 启动及派发前复核。记录请求值与实际回报，未回报记未知。禁止自动升档或静默换型。子 Agent 上下文独立，未参与制作的验收者只读。B 仅能直接派发子 Agent，子 Agent 不派生，B 不创建 Keeper/协调任务，在当前任务向 A 回报。

派发实施时 B 同步审查验收契约与需求映射；派发 P1 验收时 B 核查范围差异与执行记录；派发 P2 验收时 B 核对 P1→P2 改动与交付覆盖；最终验收时 B 核查工作区基线差异、执行记录和原始需求覆盖。必须是实际必要独立工作，若不存在则报告派发条件缺口，不虚构并行。复用角色模型与独立性合适的子 Agent。

用户已授权持续接续，R01 仅 T01，R02 仅 T02，R03 最终验收，无需逐轮确认。首次没有问题尝试历史；同一问题最多一次有新证据纠正重试，累计关联问题，不因换轮次重置。无新证据停止重试。回报编号、工作区、产物与 SHA256、每项验收证据、未验证项、剩余范围、累计尝试及在途工作。

启动 Prompt R01：B 读取本方案和项目指令，核对工作区与能力，创建自己的唯一执行记录。只完成 T01 并独立验收，在当前任务回报 A 后等待 R02。只读当前 Skill 包 /Users/kaka/kDevelop/Skills/kCrazyLuna/skills/luna-roadmap/ 与 luna-keeper/，不使用 worktree 旧副本。执行协议如下。


将本节适用规则完整写入交接方案，作为执行协议；启动 Prompt 引用该协议。

### 分工与派发

- **协调者**：统一派发、集成、维护执行记录并处理升级。
- **任务角色**：按任务能力、输入、产物及读写范围设置；职责相近且无分离需要时合并，角色数量与 Agent 实例数量分别按职责和就绪工作量确定。
- **独立验收者**：由具备相应能力、未参与被验收产物制作的 Agent 担任，在独立上下文核对权威资料及实际产物，不只采信制作方摘要，不修改验收对象。可合并验收同批任务。

派发层级固定为“执行协调者 → 子 Agent”：协调者直接派发实施、测试、诊断及独立验收 Agent；子 Agent 不创建子 Agent 或独立任务，需要拆分或调整设计时向协调者回报证据与建议。协调者不自行创建新的 Keeper 或执行协调任务。由 Keeper 编排时向指定 Keeper 回报，Roadmap 单独使用时向用户回报；模型变化不改变这些角色权限。

每次子任务派发明确当前角色、任务范围、无继续派发权限及具体回报对象，并携带关联问题的尝试记录与适用限制。只传任务所需上下文及可访问的协议引用，不把协调者启动 Prompt 当作子任务指令原样下发，也不将上级授权视为子 Agent 的派发授权。

执行会话启动及实际派发前复核方案中的接口、完整参数组合、上下文隔离和派发条件，传入任务卡、权威资料及必要上下文。将子 Agent 的模型与档位落实到派发参数，不仅写在任务文字中；核对上下文继承方式，避免意外继承协调者模型。宿主或能力变化时按方案携带的适配规则重新核对，调整执行安排。记录请求模型、档位与宿主实际回报，未回报则标明未知；不符时报告差异，按已授权规则处理，不静默替换。

并发受独立就绪任务数、用户指定的上限和宿主剩余名额共同约束，按宿主口径计算占用；上限未知时先串行，每次派发仍须满足宿主前提。

### 验收标准与证据

为验收标准设置稳定 ID（如 `AC01`），明确前置条件、输入/操作、预期结果、验证方式及证据，维护 `需求 → Txx → ACxx → 证据` 的对应关系，保留独立需求及层级。

1. 核对方案与标准是否覆盖原始需求及已确认决策。遗漏、矛盾或错误交由协调者按升级规则修正，不能以符合错误方案判定通过。
2. 停止对待验收产物的写入，用提交、文件摘要或快照标识实际内容；有未提交修改时不能仅用 HEAD。其他不冲突任务可继续。
3. 独立检查实际差异并运行必要验证，记录产物版本、逐项 AC 结果、命令/操作及证据。区分静态检查、测试和实际运行，未执行项标为未验证。
4. 交付前核对产物与验收版本一致。产物或前置依赖变化时，协调者确定受影响的任务和 AC，更新其状态并安排必要的重新验收；影响尚未查明时，暂停使用已变化输入的任务及其完成判定。

只有范围内任务经独立验收、最终集成状态满足全部必需 AC、本轮引入的差异均属于授权范围，且基线已有的无关改动保持原样、证据完整，才能宣布目标完成。必需验证缺失或只完成部分就绪任务时，报告已完成部分与剩余阻塞；范围调整须有用户明确授权。

### 进度、恢复与升级

协调者维护唯一执行记录，包含 `任务 ID | 负责人/模型 | 状态 | 产物版本/证据 | 阻塞原因/下一步`。状态为 `TODO → RUNNING → VERIFYING → PASS`；失败进入 `REWORK`，缺条件进入 `BLOCKED`。`PASS` 必须有独立验收证据，返工或解锁后按流程恢复。

在该记录中集中维护当前状态，原位更新当前轮次、方案与产物版本、任务状态、证据和下一步；阶段切换时移除旧阶段的“当前”标识。过程事件可按轮次保留为历史，但历史状态和下一步不再充当当前结论，不以追加一条 PASS 代替更新当前状态。

子 Agent 回报任务 ID、产物、验证结果、证据及阻塞。协调者在派发、验收、返工、阻塞和完成时更新记录，回报前核对当前状态与实际产物、独立验收证据及在途工作一致，再简短汇报实际派发的任务与请求模型；未派发时说明原因。记录未成功更新时说明未同步项，不把交接记录视为已齐全。续接先核对方案、记录、实际工作区和在途 Agent，避免重复派发；规划阶段仅指定记录位置，不创建空记录。

失败后在原执行记录中按稳定问题标识关联任务与 AC，维护修复及诊断的累计次数、每次方法与结果证据、剩余缺口、适用限制和下一步。一次修复尝试包括实施该方法及验证，实际诊断单独计次并纳入同一问题的总尝试；开始时登记，结束时补结果，中断或结果未知也保留已启动尝试，恢复同一次在途工作不重复计次。等待、状态查询和进度回报不计次。重新规划、换模型、换 Agent、换窗口或任务拆分重命名均保留同一问题的历史；不同的新问题分别记录，不靠改名或改称诊断重置计数。

失败后的下一次动作必须说明：上次新增事实、这次改变的方法或尚未排除的原因、预期证据，以及继续或停止的判断条件。AC 缺口缩小、具体原因被排除或必要前提得到补齐可作为进展；有进展且未触及适用限制时继续修复与验证。日志增多、改写描述或换执行者本身不算进展。重复失败且无进展时停止重复派发，由协调者评估尚未执行且有信息价值的诊断；有则执行并记录，无则交回规划者或用户，说明阻塞与恢复条件。设计前提失效时带证据修订，其他已授权、未触及适用限制且不受阻任务继续。

沿用用户已有尝试或预算限制；需要进一步控制消耗时在方案中明确上限、计数口径及适用范围（单问题、任务或整体目标），不统一写死次数。没有数值上限仍执行上述无进展停止规则。达到上限后停止受该限制约束的派发并报告未完成范围与恢复条件，不自行重置或提高上限；单问题上限不阻止其他独立工作，整体上限则适用于全目标。只有宿主提供可信用量时才按真实 token 或费用判断；必要预算无法核实时说明缺口，与用户确定可核实的限制，不以尝试次数冒充精确费用控制。

有相应能力与授权的协调者可在既定范围和产品契约内修订方案、补齐已解锁阶段；否则交回规划者或用户，相关任务保持阻塞。因失败修订时说明需改变的设计前提及证据，不能仅因失败重写方案再从头尝试。修订需更新方案版本、受影响任务及验收状态，保留关联问题的历史与限制，不擅自扩大模型候选范围或放宽验收要求。
